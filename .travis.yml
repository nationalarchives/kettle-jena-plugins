language: java

matrix:
  include:
    - jdk: openjdk8
    - jdk: openjdk11

    - os: osx
      osx_image: xcode9.3
      env:
        - JAVA_HOME=$(/usr/libexec/java_home)

    - os: windows

cache:
  directories:
    - $HOME/.m2